# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "processes-pattern-matching"
kind = "regression"
covers = ["Structure patterns, match-time variable snapshots, short-circuited condition-list effects, conditional-expression bindings, and filtered pattern-case priority preserve procedural semantics."]
category = "processes"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
equivalence = true

[assertions]
min_ports = 13
min_cells = 1
