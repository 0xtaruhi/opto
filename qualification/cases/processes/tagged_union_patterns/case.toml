# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "processes-tagged-union-patterns"
kind = "regression"
covers = ["Packed and unpacked tagged unions retain a canonical discriminant and payload across runtime construction, pattern case, conditional matching, and variable binding."]
category = "processes"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
equivalence = true

[assertions]
min_ports = 7
min_cells = 1
