# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "processes-activation-loop-exit"
kind = "regression"
covers = ["Unbounded and runtime-entry loops terminate through current-function return or an enclosing lexical disable without confusing nested activations."]
category = "processes"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
equivalence = true

[assertions]
min_ports = 5
min_cells = 1
