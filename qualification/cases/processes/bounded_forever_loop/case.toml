# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "processes-bounded-forever-loop"
kind = "regression"
covers = ["A forever loop retains runtime break and continue semantics while local induction state proves a finite unconditional exit."]
category = "processes"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
equivalence = true

[assertions]
min_ports = 4
min_cells = 1
