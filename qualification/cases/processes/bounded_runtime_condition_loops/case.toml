# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "processes-bounded-runtime-condition-loops"
kind = "regression"
covers = ["Runtime early-exit predicates in for, while, and do-while loops retain a statically proved expansion bound."]
category = "processes"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
equivalence = true

[assertions]
min_ports = 8
min_cells = 1
