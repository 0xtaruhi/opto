# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "processes-runtime-bound-loops"
kind = "regression"
covers = ["Finite signed and unsigned runtime values bound for, while, and do-while expansion through conservative ProcGraph range facts."]
category = "processes"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
equivalence = true

[assertions]
min_ports = 7
min_cells = 1
