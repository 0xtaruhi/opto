# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "sequential-edge-async-control-udp"
kind = "regression"
covers = ["A sequential UDP with one data-clock transition input and a distinct edge-coded constant asynchronous control lowers to one resettable register."]
category = "sequential"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
sequential = true
equivalence = true
equivalence_initial_state = "zero"

[assertions]
min_ports = 4
min_cells = 1
