# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "sequential-dual-edge-state"
kind = "regression"
covers = ["One state variable updated on both edges of the same clock lowers to opposite-edge phase banks with exact conditional-hold semantics."]
category = "sequential"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.v"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
sequential = true
equivalence = true
equivalence_initial_state = "zero"

[assertions]
min_ports = 5
min_cells = 1
