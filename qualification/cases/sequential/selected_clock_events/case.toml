# SPDX-FileCopyrightText: 2026 Zhengyi Zhang
# SPDX-License-Identifier: GPL-3.0-only

format = 1
id = "sequential-selected-clock-events"
kind = "regression"
covers = ["Static and runtime bit-selected clocks remain exact scalar event values, and duplicate dynamic events OR their independent iff qualifiers."]
category = "sequential"
language = "sverilog"
top = "top"
sources = ["top.sv"]
equivalence_sources = ["reference.sv"]
flow = "synth"
library = "../../../libraries/opto_test.lib"
sequential = true
equivalence = true

[assertions]
min_ports = 9
min_cells = 2
